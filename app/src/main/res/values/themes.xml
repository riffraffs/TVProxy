<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- P0 uses a plain activity, not BrowseSupportFragment. Theme.Leanback
         paints an empty black window over setContentView layouts. -->
    <style name="Theme.TVProxy" parent="@android:style/Theme.NoTitleBar.Fullscreen">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="android:textColor">@color/on_background</item>
    </style>

    <!-- 自绘卡片作弹窗外观：去掉系统默认窗框，避免鸿蒙 TV 在自定义卡片外再画一圈浅色描边/辉光。 -->
    <style name="Theme.TVProxy.Dialog" parent="@android:style/Theme.DeviceDefault.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
